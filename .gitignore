__pycache__/
*.pyc

# Personal. Never goes to the repository.
# Thresholds and mode of this machine
/config.json
# Trail of watchdog firings: paths, projects, session identifiers
/events.jsonl
# Friction notes naming real projects. Anchored with a leading slash: an
# unanchored pattern matches at any depth, and `templates/FEEDBACK.md` —
# which IS published — survived only by already being tracked.
/FEEDBACK.md
# Session journals
/journal/
# Filled-in frames of real projects
/examples/
# Personal drafts and earlier versions of commands. The whole directory: holding the
# line by file name meant the next personal command would travel on the first publish.
/legacy/
/commands/
# Personal layer: own glossary and own skills, laid over the public ones
/skills-local/
